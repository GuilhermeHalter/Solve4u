# URL de conexão com o banco de dados PostgreSQL hospedado na Supabase
spring.datasource.url=jdbc:postgresql://aws-0-us-west-1.pooler.supabase.com:6543/postgres

# Credenciais de acesso ao banco de dados
spring.datasource.username=postgres.nnevllxtukqsphaoouih
spring.datasource.password=ProjectSolve4u2025

# Driver JDBC para PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Dialeto do Hibernate para PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# HikariCP (pool de conexões)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# Mostrar as SQLs no log
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
